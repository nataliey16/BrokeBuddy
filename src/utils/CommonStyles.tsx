import {StyleSheet} from 'react-native';

export const CommonStyles = StyleSheet.create({
  screens: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },

  txt: {
    fontSize: 30,
    color: '#000',
    margin: 10,
  },
});
